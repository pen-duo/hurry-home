<view class="container">
  <view
    wx:for="{{count}}"
    wx:key="index"
    class="item"
  >
    <i-icon
      name="{{icon}}"
      size="{{size}}"
      color="{{index < selected ? selectedColor : defaultColor}}"
    ></i-icon>
  </view>
</view>

