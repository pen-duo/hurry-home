<view
  class="container"
  wx:for="{{count}}"
  wx:key="{{index}}"
>
  <view class="item">
    <i-icon
      name="{{icon}}"
      size="{{size}}"
      color="{{index < selected ? selectedColor : defaultColor}}"
    ></i-icon>
  </view>
</view>

